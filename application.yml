logging:
  level:
    org:
      springframework:
        web: DEBUG
    root: INFO

spring:
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: TRUE
  h2:
    console:
      enabled: true
      path: /h2

springfox:
  documentation:
    swagger:
      v2:
        path: /api/api-docs